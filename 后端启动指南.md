# 废品回收APP 后端启动指南

**项目名称**: recycle-app-backend  
**技术栈**: Spring Boot + MyBatis-Plus + MySQL  
**数据库**: huanbao  
**更新时间**: 2024-12-18

---

## 📋 前置条件检查

### 1. 环境要求
- ✅ JDK 8 或以上版本
- ✅ Maven 3.6 或以上版本
- ✅ MySQL 5.7 或以上版本
- ✅ Redis（可选，用于缓存）

### 2. 验证环境
```bash
# 检查Java版本
java -version

# 检查Maven版本
mvn -version

# 检查MySQL服务状态
# Windows:
net start | findstr MySQL

# 检查MySQL连接
mysql -u root -p
```

---

## 🗄️ 数据库初始化

### 步骤1：登录MySQL
```bash
mysql -u root -p
# 输入密码：xfj7758758@
```

### 步骤2：创建数据库
```sql
CREATE DATABASE IF NOT EXISTS huanbao DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE huanbao;
```

### 步骤3：执行SQL脚本

**方式一：使用SOURCE命令（推荐）**
```sql
USE huanbao;
SOURCE e:/Recycle3/recycle-app/database/recycle_app_database.sql;
SOURCE e:/Recycle3/recycle-app/database/recycle_app_business_database.sql;
SOURCE e:/Recycle3/recycle-app/database/recycle_app_government_database.sql;
```

**方式二：使用MySQL Workbench**
1. 打开MySQL Workbench
2. 连接到数据库服务器
3. File → Run SQL Script
4. 依次选择并执行三个SQL文件

**方式三：命令行导入**
```bash
mysql -u root -pxfj7758758@ huanbao < e:/Recycle3/recycle-app/database/recycle_app_database.sql
mysql -u root -pxfj7758758@ huanbao < e:/Recycle3/recycle-app/database/recycle_app_business_database.sql
mysql -u root -pxfj7758758@ huanbao < e:/Recycle3/recycle-app/database/recycle_app_government_database.sql
```

### 步骤4：验证表创建
```sql
USE huanbao;
SHOW TABLES;
-- 应该显示61张表

SELECT COUNT(*) FROM information_schema.tables 
WHERE table_schema = 'huanbao';
-- 结果应该是61
```

---

## ⚙️ 配置文件检查

### 验证 application.yml 配置
文件位置：`backend/src/main/resources/application.yml`

关键配置项：
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/huanbao?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: xfj7758758@
```

### Redis配置（如果使用）
```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
```

---

## 🚀 启动后端应用

### 方式一：使用Maven命令（推荐）

```bash
# 进入后端目录
cd e:/Recycle3/recycle-app/backend

# 清理并编译
mvn clean install -DskipTests

# 启动应用
mvn spring-boot:run
```

### 方式二：使用Maven Wrapper

```bash
cd e:/Recycle3/recycle-app/backend

# Windows:
.\mvnw.cmd spring-boot:run

# Linux/Mac:
./mvnw spring-boot:run
```

### 方式三：直接运行JAR包

```bash
cd e:/Recycle3/recycle-app/backend

# 先打包
mvn clean package -DskipTests

# 运行JAR
java -jar target/recycle-app-backend-0.0.1-SNAPSHOT.jar
```

### 方式四：使用IDE运行

**IDEA / Eclipse:**
1. 打开项目：`backend`
2. 找到主类：`RecycleAppApplication.java`
3. 右键 → Run 'RecycleAppApplication'

---

## ✅ 验证启动状态

### 1. 查看控制台日志

启动成功的关键日志：
```
Started RecycleAppApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)
```

### 2. 测试数据库连接

查看日志中是否有MyBatis-Plus加载Mapper的信息：
```
Mapped "{[XXXMapper]}" onto ...
```

### 3. 访问Swagger文档

浏览器打开：
```
http://localhost:8080/doc.html
```

应该能看到Knife4j的API文档界面，包含：
- Client端接口（AuthController、UserController等）
- Business端接口（BusinessAuthController、BusinessOrderController等）
- Government端接口（GovernmentAuthController、StatisticsController等）

### 4. 健康检查

访问Spring Boot Actuator健康检查端点（如果配置）：
```
http://localhost:8080/actuator/health
```

### 5. 测试基础API

使用Postman或curl测试：

**测试1：获取废品分类列表**
```bash
curl http://localhost:8080/api/client/categories
```

**测试2：获取回收员列表**
```bash
curl http://localhost:8080/api/client/collectors
```

---

## 🐛 常见问题排查

### 问题1：数据库连接失败

**错误信息**：
```
Communications link failure
```

**解决方案**：
1. 检查MySQL服务是否启动
2. 验证数据库配置（用户名、密码、数据库名）
3. 确认MySQL监听端口（默认3306）
4. 检查防火墙设置

**验证命令**：
```bash
mysql -u root -pxfj7758758@ -h localhost -P 3306 -e "SELECT 1;"
```

### 问题2：端口被占用

**错误信息**：
```
Port 8080 was already in use
```

**解决方案**：
```bash
# Windows查看端口占用
netstat -ano | findstr :8080

# 杀死进程（使用上一步查到的PID）
taskkill /PID <PID> /F

# 或修改application.yml中的端口
server:
  port: 8081
```

### 问题3：Mapper未找到

**错误信息**：
```
Invalid bound statement (not found)
```

**解决方案**：
1. 检查Mapper接口上是否有`@Mapper`注解
2. 确认主类上有`@MapperScan("com.recycle.mapper")`
3. 清理并重新编译：`mvn clean install`

### 问题4：Maven依赖下载失败

**解决方案**：
```bash
# 清理本地仓库
mvn dependency:purge-local-repository

# 强制更新
mvn clean install -U

# 或配置阿里云镜像（修改settings.xml）
```

### 问题5：内存不足

**错误信息**：
```
java.lang.OutOfMemoryError: Java heap space
```

**解决方案**：
```bash
# 增加JVM内存
java -Xms512m -Xmx1024m -jar target/recycle-app-backend-0.0.1-SNAPSHOT.jar

# 或在MAVEN_OPTS中设置
set MAVEN_OPTS=-Xmx1024m
mvn spring-boot:run
```

---

## 📊 数据库表结构概览

### C端表（26张）
- 用户管理：users, user_addresses, user_badges, user_tasks
- 订单系统：recycle_orders, order_reviews
- 积分商城：products, points_records, exchange_records
- 社交功能：invite_records, checkin_records, activities
- 其他：waste_categories, collectors, drop_points, notifications等

### B端表（18张）
- 企业管理：enterprises, enterprise_employees, contracts
- 订单管理：business_orders
- 设备管理：devices, device_maintenance, device_alerts
- 数据统计：statistics_reports, monitoring_locations
- AI功能：ai_chat_history, ai_insights
- 其他：subscription_plans, policies等

### G端表（17张）
- 政府管理：government_users, administrative_regions
- 监管功能：government_warnings, recycle_stations, compliance_checks
- 数据分析：industry_chain_data, region_statistics, category_statistics
- 补贴管理：subsidy_applications, subsidy_payments
- 政策评估：policy_evaluations, policy_simulations
- AI报告：ai_generated_reports

**总计：61张表，全部已对接完成！**

---

## 🔧 开发调试技巧

### 1. 开启SQL日志
在`application.yml`中已配置：
```yaml
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
```

可以在控制台看到所有执行的SQL语句。

### 2. 使用调试端点

访问以下端点查看应用信息：
```
http://localhost:8080/actuator/beans    # Bean列表
http://localhost:8080/actuator/mappings # URL映射
http://localhost:8080/actuator/env      # 环境变量
```

### 3. 热部署配置

添加spring-boot-devtools依赖实现热部署：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <optional>true</optional>
</dependency>
```

### 4. 日志级别调整

在`application.yml`中调整日志级别：
```yaml
logging:
  level:
    com.recycle: DEBUG
    com.baomidou.mybatisplus: DEBUG
```

---

## 📝 API测试示例

### 使用Postman测试集合

创建以下测试请求：

**1. 用户注册**
```
POST http://localhost:8080/api/client/auth/register
Content-Type: application/json

{
  "phone": "13800138000",
  "password": "123456",
  "nickname": "环保达人"
}
```

**2. 用户登录**
```
POST http://localhost:8080/api/client/auth/login
Content-Type: application/json

{
  "phone": "13800138000",
  "password": "123456"
}
```

**3. 获取废品分类**
```
GET http://localhost:8080/api/client/categories
Authorization: Bearer {token}
```

**4. 创建回收订单**
```
POST http://localhost:8080/api/client/orders
Content-Type: application/json
Authorization: Bearer {token}

{
  "categoryId": 1,
  "weight": 5.5,
  "addressId": 1,
  "remark": "请尽快上门"
}
```

---

## 🎯 下一步建议

### 1. 初始化基础数据
SQL脚本中已包含部分初始数据：
- 废品分类（8种）
- 任务列表（4个）
- 捐赠项目（3个）
- 徽章定义（6个）

可根据需要添加更多测试数据。

### 2. 配置Redis缓存
如需使用Redis缓存，启动Redis服务：
```bash
# Windows
redis-server.exe

# Linux
redis-server
```

### 3. 集成前端
- 确保前端项目（Angular）正在运行
- 检查CORS配置是否正确
- 验证API接口调用

### 4. 性能优化
- 配置数据库连接池
- 启用查询缓存
- 添加索引优化
- 配置Nginx反向代理

---

## 📞 技术支持

如遇到问题，请检查：
1. ✅ MySQL服务是否运行
2. ✅ 数据库配置是否正确
3. ✅ 所有61张表是否创建成功
4. ✅ 端口8080是否被占用
5. ✅ Maven依赖是否下载完整

---

## ✅ 启动检查清单

- [ ] MySQL服务已启动
- [ ] 数据库huanbao已创建
- [ ] 61张表全部创建成功
- [ ] application.yml配置正确
- [ ] Maven依赖已下载
- [ ] 后端应用启动成功（端口8080）
- [ ] Swagger文档可访问
- [ ] 基础API测试通过
- [ ] 前端可正常调用后端接口

**祝您开发顺利！** 🎉
